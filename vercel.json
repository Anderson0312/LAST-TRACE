{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "if [ ! -x \"$HOME/flutter/bin/flutter\" ]; then git clone --depth 1 --branch stable https://github.com/flutter/flutter.git \"$HOME/flutter\"; fi && export PATH=\"$HOME/flutter/bin:$PATH\" && git config --global --add safe.directory \"$HOME/flutter\" && flutter config --no-analytics && flutter precache --web && flutter pub get",
  "buildCommand": "export PATH=\"$HOME/flutter/bin:$PATH\" && flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/((?!assets/|canvaskit/|icons/|flutter).*)", "destination": "/index.html" }
  ]
}
